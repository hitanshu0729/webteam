<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Subject Marks</title>
    <!-- Include Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f8f9fa;
        }

        .container {
            background-color: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin-top: 50px;
        }

        h1, h2 {
            color: #007bff;
        }

        table {
            width: 100%;
            margin-top: 20px;
            border-collapse: collapse;
        }

        th, td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #dee2e6;
        }

        th {
            background-color: #007bff;
            color: white;
        }

        input[type="number"] {
            width: 80px;
            text-align: center;
        }

        button {
            font-size: 16px;
            padding: 8px;
            width: 100px;
            border: 1px solid #007bff;
            border-radius: 5px;
            background-color: #007bff;
            color: white;
            cursor: pointer;
        }

        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    {{> navbar}}

    <div class="container mt-4">
        <h1 class="mb-4">Subject Marks</h1>

        <!-- Select Subject Dropdown -->
        <div class="mb-3">
            <label for="subjectSelect" class="form-label">Select Subject:</label>
            <select class="form-select" id="subjectSelect" name="subjectSelect" required>
                <!-- Populate this dropdown dynamically with available subjects -->
                <option value="subject1">Subject 1</option>
                <option value="subject2">Subject 2</option>
                <!-- Add more options as needed -->
            </select>
        </div>

        <!-- Display Marks of Students -->
        <h2 class="mt-4 mb-3">Students and Marks:</h2>
        <table class="table">
            <thead class="table-primary">
                <tr>
                    <th>Registration Number</th>
                    <th>Name</th>
                    <th>Marks</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <!-- Iterate over the list of students for the selected subject -->
                {{#each students}}
                <tr>
                    <td>{{this.registrationNumber}}</td>
                    <td>{{this.name}}</td>
                    <td>
                        <input type="number" value="{{this.marks}}" id="marks-{{this.registrationNumber}}" />
                    </td>
                    <td>
                        <button onclick="updateMarks('{{this.registrationNumber}}')">Update</button>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>

    <!-- Include Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function updateMarks(registrationNumber) {
            // Retrieve the updated marks for the student with the given registration number
            var updatedMarks = document.getElementById(`marks-${registrationNumber}`).value;

            // Implement logic to update the marks in the database or perform other actions
            // You can use AJAX to send the updated marks to the server

            // Example:
            // $.ajax({
            //     type: "POST",
            //     url: "/updateMarks",
            //     data: {
            //         registrationNumber: registrationNumber,
            //         marks: updatedMarks
            //     },
            //     success: function (response) {
            //         // Handle success, if needed
            //         alert("Marks updated successfully");
            //     },
            //     error: function (error) {
            //         // Handle error, if needed
            //         alert("Failed to update marks");
            //     }
            // });
        }
    </script>
</body>
</html>
